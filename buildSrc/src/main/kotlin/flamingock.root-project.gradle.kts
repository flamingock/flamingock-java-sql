/**
 * Flamingock root project configuration plugin.
 * Handles root-level plugin applications.
 */

plugins {
    kotlin("jvm") apply false
    id("com.diffplug.spotless") apply false
    id("org.jreleaser")
}